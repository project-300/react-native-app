import { FAKE_ACTION_TYPE } from '../constants/redux-actions';

/*
* This file will be used for exporting all actions.
* Temporarily added an example fake actions.
* */

export const fakeAction: object = () => {
	return {
		type: FAKE_ACTION_TYPE
	};
};
